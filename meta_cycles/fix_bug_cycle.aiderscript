/open path/to/buggy_file.py

/ask We have a session bug that throws an exception after 30 minutes of idle time. Please fix session_handling() so it gracefully handles idle sessions. Then write or update tests for it.

/code Let's add a None-check in session_handling() and update tests in test_sessions.py

/run pytest --maxfail=1

/ask If all tests pass, let's open meta_cycle.json and set "Fix Bug Cycle" => "completed": true

/open meta_cycle.json

/code "Fix Bug Cycle" -> "completed": true

/ask Done. Do you want to reload the manager?

/code Sure, let's do that. 
/load meta_cycle_manager.aiderscript
