# Console Commands for Step 3 (Testing)
# Only edit the section between CODE START and CODE END

### CODE START ###

# Configure paths with strict verification
/run scripts/path_config.py --strict --verify

# Initialize logging with test settings
/run scripts/init_logging.py --test

# Setup test environment with schema validation
/run scripts/setup_test_env.py --test --validate-schema

# Install test dependencies with retry
/run scripts/install_test_dependencies.py --retry=3

# Initialize test database with strict mode
/run scripts/startup/init_db.py --test --strict --retry=5

# Run comprehensive test suites
/run scripts/testing/run_tests.py --unit --integration --e2e

# Generate detailed coverage report
/run scripts/generate_coverage_report.py --html --xml

# Verify critical systems with enhanced checks
/run scripts/verification/verify_security.py --full-audit
/run scripts/verification/verify_db_connection.py --retry=5 --strict
/run scripts/verification/verify_db_schema.py --strict --verify-relations
/run scripts/verification/verify_backup.py --retention=7 --verify-size
/run scripts/verification/verify_monitoring.py --production --verify-alerts

# Verify test coverage with critical paths
/run scripts/verification/verify_test_coverage.py --threshold=85 --critical-paths

# Verify logging configuration
/run scripts/verification/verify_logs.py --check-rotation --verify-archives

# Commit test results with verification
/run scripts/commit_changes.py --message "Testing cycle completed" --verify

### CODE END ###

# Console commands done, we go on to 07_STEP_4.txt
/load automation/07_STEP_4.txt
