# Console Commands for Step 3 (Testing)
# Only edit the section between CODE START and CODE END

### CODE START ###

# Configure paths with enhanced verification
/run scripts/path_config.py --strict --verify --test

# Initialize logging with test settings
/run scripts/init_logging.py --test --verbose

# Setup test environment with schema validation
/run scripts/setup_test_env.py --test --validate-schema --retry=3

# Install test dependencies with retry
/run scripts/install_test_dependencies.py --retry=3 --strict

# Initialize test database with strict mode
/run scripts/startup/init_db.py --test --strict --retry=5 --validate

# Run comprehensive test suites with coverage
/run scripts/testing/run_tests.py --unit --integration --e2e --cov

# Generate detailed coverage report
/run scripts/generate_coverage_report.py --html --xml --threshold=85

# Verify admin functionality
/run scripts/verification/verify_htmx_integration.py --full --test-all-crud --admin-only
/run scripts/verification/verify_security.py --full-audit --admin-only --validate-keys --htmx
/run scripts/verification/verify_db_schema.py --strict --admin-tables --verify-relations
/run scripts/verification/verify_test_coverage.py --threshold=85 --admin-only --critical-paths

# Verify core systems
/run scripts/verification/verify_db_connection.py --retry=5 --timeout=30 --strict
/run scripts/verification/verify_backup.py --retention=7 --verify-integrity --test-restore
/run scripts/verification/verify_monitoring.py --production --alert-thresholds --detailed-metrics
/run scripts/verification/verify_logs.py --check-rotation --verify-archives --admin-actions
/run scripts/verification/verify_config.py --production --validate-env --strict

# Commit test results with verification
/run scripts/commit_changes.py --message "Testing cycle completed" --verify --strict

### CODE END ###

# Console commands done, we go on to 07_STEP_4.txt
/load automation/07_STEP_4.txt
