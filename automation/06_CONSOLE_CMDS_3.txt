# Console Commands for Step 3 (Testing)
# Only edit the section between CODE START and CODE END

### CODE START ###

# Initialize test environment with proper path configuration
/run scripts/path_config.py --test
/run scripts/init_logging.py --test
/run scripts/setup_test_env.py --test

# Initialize test database with schema validation
/run scripts/startup/init_db.py --test --validate-schema --retry=3
/run scripts/startup/init_admin.py --test --strict-password --verify-email --retry=3

# Core functionality tests with enhanced verification
/run scripts/verification/verify_db_connection.py --test --strict --retry=3 --validate-schema
/run scripts/verification/verify_db_schema.py --test --strict --tables=stipend,tag,organization,user --validate-required-fields --validate-relations --retry=3

# Security verification with daily checks
/run scripts/verification/verify_security.py --test --full-audit --check-path-config --daily --check-rate-limits

# Feedback system verification
/run scripts/verification/verify_feedback.py --test --check-surveys --check-analytics

# HTMX interface tests
/run scripts/verification/verify_htmx_integration.py --test --full --test-stipends-crud

# Run comprehensive test suites
/run scripts/testing/run_tests.py --unit --integration --e2e --cov --cov-report=html

# Generate and verify coverage reports
/run scripts/generate_coverage_report.py --test --html --xml
/run scripts/verification/verify_test_coverage.py --test --module=stipend --threshold=85 --critical-paths

# Finalize testing with verification
/run scripts/commit_changes.py --message "Testing cycle completed" --verify --push

### CODE END ###

# Console commands done, we go on to 07_STEP_4.txt
/load automation/07_STEP_4.txt
