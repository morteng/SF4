# Console Commands for Step 3 (Testing)
# Only edit the section between CODE START and CODE END - always run scripts using python, e.g. /run python scripts/myscript.py

### CODE START ###

# Validate user confirmation system
/run python -m pytest tests/test_user_model.py -k "confirmed_at or password" -v --cov=app.models.user --cov-report=term-missing

# Security audit for confirmation flow
/run python scripts/verification/verify_security.py --check-auth --check-confirmed

# Final schema validation
/run python scripts/verification/verify_db_schema.py --full-check --production

# Commit verified state
/run python scripts/commit_changes.py --message "Cycle 102: User confirmation system validation"

### CODE END ###

# Console commands done, we go on to 07_STEP_4.txt
/load automation/07_STEP_4.txt
