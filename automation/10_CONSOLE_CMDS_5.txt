# Console Commands for Step 5 (Document, Demo, Deploy)
# Only edit the section between CODE START and CODE END

### CODE START ###

# Initialize production environment
/run scripts/init_logging.py --production
/run scripts/setup_test_env.py --production

# Verify core functionality
/run scripts/verification/verify_db_connection.py --production --strict
/run scripts/verification/verify_db_schema.py --production --strict --tables=stipend

# Security verification
/run scripts/verification/verify_security.py --production --full-audit

# Configuration verification
/run scripts/verification/verify_config.py --production --strict

# Test coverage verification
/run scripts/verification/verify_test_coverage.py --production --module=stipend --threshold=85

# Backup and monitoring verification
/run scripts/verification/verify_backup.py --production --verify-integrity
/run scripts/verification/verify_monitoring.py --production

# Production readiness verification
/run scripts/verification/verify_production_ready.py --production --full

# Documentation finalization
/run scripts/finalize_docs.py
/run scripts/generate_deployment_checklist.py

# Request final approval
/run scripts/request_approval.py

# Commit and push final changes
/run scripts/commit_changes.py --message "Production deployment v1.2.11" --verify --push

# Run application for management review - do not do this yet!
# /run python app.py

### CODE END ###

# Go to cleanup step, important! 
/load automation/11_FINAL_CLEANUP.txt
