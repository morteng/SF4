# Console Commands for Step 2 (Coding)
# Do not use echo
# Only edit the section between CODE START and CODE END

### CODE START ###

# Initialize core systems with proper path configuration
/run scripts/path_config.py --production --verify
/run scripts/init_logging.py --production
/run scripts/startup/init_db.py --validate-schema --production
/run scripts/startup/init_admin.py --strict-password --verify-email

# Verify system configuration with enhanced checks
/run scripts/verification/verify_config.py --production --strict --check-admin-config
/run scripts/verification/verify_db_schema.py --validate-required-fields --check-admin-interface
/run scripts/verification/verify_security.py --check-rate-limits --daily

# Verify test coverage and production readiness
/run scripts/verification/verify_test_coverage.py --threshold=85 --critical-paths
/run scripts/verification/verify_production_ready.py

# Verify system health and monitoring
/run scripts/verification/verify_health.py --production --check-response-time
/run scripts/verification/verify_monitoring.py --check-dashboards --check-alerts

# Finalize changes with verification
/run scripts/commit_changes.py --message "Step 2 coding completed" --verify --push

### CODE END ###

# Console commands done, we go on to 05_STEP_3.txt
/load automation/05_STEP_3.txt
