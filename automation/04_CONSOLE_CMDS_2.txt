# Console Commands for Step 2 (Coding)
# Do not use echo
# Only edit the section between CODE START and CODE END

### CODE START ###

# Configure paths with enhanced error handling
/run scripts/path_config.py --strict --verify

# Initialize logging with production settings
/run scripts/init_logging.py --production

# Initialize database with enhanced schema validation
/run scripts/startup/init_db.py --validate-schema --retry=5 --strict

# Verify security settings with full audit and admin focus
/run scripts/verification/verify_security.py --full-audit --admin-only --validate-keys

# Initialize admin user with strict password checks
/run scripts/startup/init_admin.py --verify-email --strict-password

# Verify database connection with retry logic
/run scripts/verification/verify_db_connection.py --retry=5 --timeout=30 --strict

# Verify database schema integrity with relation validation
/run scripts/verification/verify_db_schema.py --strict --admin-tables --verify-relations

# Verify test coverage with admin focus
/run scripts/verification/verify_test_coverage.py --threshold=85 --admin-only --critical-paths

# Verify HTMX integration for admin CRUD operations
/run scripts/verification/verify_htmx_integration.py --full --test-all-crud --admin-only

# Verify backup integrity with retention policy
/run scripts/verification/verify_backup.py --retention=7 --verify-integrity

# Verify monitoring configuration with alert thresholds
/run scripts/verification/verify_monitoring.py --production --alert-thresholds

# Verify logging configuration
/run scripts/verification/verify_logs.py --check-rotation --verify-archives

# Verify production configuration
/run scripts/verification/verify_config.py --production --validate-env

# Commit changes with verification summary
/run scripts/commit_changes.py --message "Step 2 verification completed" --verify

### CODE END ###

# Console commands done, we go on to 05_STEP_3.txt
/load automation/05_STEP_3.txt
