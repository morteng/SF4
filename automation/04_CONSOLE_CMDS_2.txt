# Console Commands for Step 2 (Coding)
# Do not use echo
# Only edit the section between CODE START and CODE END

### CODE START ###

# Initialize core systems with proper path configuration
/run scripts/init_logging.py --production
/run scripts/path_config.py --production
/run scripts/startup/init_db.py --validate-schema --production
/run scripts/startup/init_admin.py --strict-password --verify-email

# Verify system configuration
/run scripts/verification/verify_config.py --production --strict --check-admin-config

# Security verification with rate limiting
/run scripts/verification/verify_security.py --full-audit --check-admin-interface --check-rate-limits --check-path-config --daily

# Database verification with retry logic
/run scripts/verification/verify_db_connection.py --strict --retry=3
/run scripts/verification/verify_db_schema.py --strict --tables=stipend,tag,organization,user --validate-required-fields --validate-relations

# HTMX interface verification
/run scripts/verification/verify_htmx_integration.py --full --test-stipends-crud --admin-interface --validate-full-reloads

# Test coverage verification with admin focus
/run scripts/verification/verify_test_coverage.py --module=stipend --threshold=85 --critical-paths --admin-only

# Backup verification with integrity checks
/run scripts/verification/verify_backup.py --verify-integrity --retry=3 --check-retention

# Monitoring verification with alert validation
/run scripts/verification/verify_monitoring.py --production --validate-alerts --check-dashboards

# Finalize changes with verification
/run scripts/commit_changes.py --message "Step 2 verification completed" --verify --push

### CODE END ###

# Console commands done, we go on to 05_STEP_3.txt
/load automation/05_STEP_3.txt
