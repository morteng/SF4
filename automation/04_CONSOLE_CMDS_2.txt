# Console Commands for Step 2 (Coding)
# Do not use echo
# Only edit the section between CODE START and CODE END

### CODE START ###

# Initialize system with enhanced logging
/run scripts/init_logging.py --production --verify
/run scripts/path_config.py --production --verify
/run scripts/start_cycle.py

# Database initialization with retries
/run scripts/startup/init_db.py --retry=5 --validate-schema --check-pool --verify-timeout

# Admin user setup with enhanced security
/run scripts/startup/init_admin.py --strict-password --verify-email --check-complexity --require-special-char

# Core system verification
/run scripts/verification/verify_db_connection.py --retries=5 --timeout=30 --check-pool
/run scripts/verification/verify_backup.py --verify-integrity --check-schema
/run scripts/verification/verify_security.py --full-audit --check-admin-interface --verify-auth

# Monitoring and health checks
/run scripts/verification/verify_monitoring.py --full --check-dashboards --verify-metrics
/run scripts/verification/verify_test_coverage.py --threshold=85 --critical-paths

# Final verification and commit
/run scripts/verification/verify_production_ready.py --full --validate-config
/run scripts/commit_changes.py --message "Step 2 coding completed" --verify --push

### CODE END ###

# Console commands done, we go on to 05_STEP_3.txt
/load automation/05_STEP_3.txt
