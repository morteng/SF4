# Console Commands for Step 1 (Analyzing)
# Do not use echo
# Only edit the section of this document between CODE START and CODE END

### CODE START ###

# Verify path configuration and imports
/run scripts/verification/verify_imports.py --full --validate-paths --root=C:\github\SF4

# Comprehensive stipends CRUD testing with full page reloads
/run scripts/verification/verify_htmx_integration.py --full --test-stipends-crud --admin-interface --validate-full-reloads

# Verify stipends database schema and constraints
/run scripts/verification/verify_db_schema.py --strict --tables=stipend --check-constraints --validate-required-fields

# Security audit focused on admin interface
/run scripts/verification/verify_security.py --full-audit --validate-keys --check-admin-interface --verify-csrf

# Verify test coverage for critical paths
/run scripts/verification/verify_test_coverage.py --module=stipend --threshold=85 --critical-paths --admin-interface --validate-critical-modules

### CODE END ###

# Console commands done, we go on to 03_STEP_2.txt
/load automation/03_STEP_2.txt
