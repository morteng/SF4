# Console Commands for Step 1 (Analyzing)
# Do not use echo
# Only edit the section of this document between CODE START and CODE END

### CODE START ###

# Comprehensive security audit with admin interface and rate limiting checks
/run scripts/verification/verify_security.py --full-audit --check-admin-interface --check-rate-limits --daily

# Verify test coverage meets 85% target with critical path analysis
/run scripts/verification/verify_test_coverage.py --threshold=85 --critical-paths --admin-only

# Verify monitoring configuration with alert thresholds
/run scripts/verification/verify_monitoring.py --check-alerts --verify-dashboards

# Verify backup integrity and retention policy
/run scripts/verification/verify_backup.py --check-retention --verify-schema

### CODE END ###

# Console commands done, we go on to 03_STEP_2.txt
/load automation/03_STEP_2.txt
