# Console Commands for Step 1 (Analyzing)
# Do not use echo
# Only edit the section of this document between CODE START and CODE END

### CODE START ###

# Comprehensive admin CRUD testing with HTMX
/run scripts/verification/verify_htmx_integration.py --full --test-all-crud --admin-only --validate-partial-updates

# Verify admin database schema and relations
/run scripts/verification/verify_db_schema.py --strict --admin-tables --verify-relations --check-htmx-endpoints

# Security audit focused on admin and HTMX
/run scripts/verification/verify_security.py --full-audit --htmx --admin-only --validate-keys --check-htmx-security

# Verify admin-specific test coverage
/run scripts/verification/verify_test_coverage.py --admin-only --critical-paths --threshold=85 --htmx-coverage

### CODE END ###

# Console commands done, we go on to 03_STEP_2.txt
/load automation/03_STEP_2.txt
