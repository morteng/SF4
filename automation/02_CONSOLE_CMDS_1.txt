# Console Commands for Step 1 (Analyzing)
# Do not use echo
# Only edit the section of this document between CODE START and CODE END

### CODE START ###

# Verify path configuration and imports
/run scripts/verification/verify_imports.py --full --validate-paths

# Comprehensive stipends CRUD testing with HTMX
/run scripts/verification/verify_htmx_integration.py --full --test-stipends-crud --validate-partial-updates

# Verify stipends database schema and relations
/run scripts/verification/verify_db_schema.py --strict --tables=stipend --verify-relations

# Security audit focused on stipends management
/run scripts/verification/verify_security.py --full-audit --validate-keys --check-stipends-security

# Verify stipends test coverage
/run scripts/verification/verify_test_coverage.py --module=stipend --threshold=95

### CODE END ###

# Console commands done, we go on to 03_STEP_2.txt
/load automation/03_STEP_2.txt
