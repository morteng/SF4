# Console Commands for Step 1 (Analyzing)
# Do not use echo
# Only edit the section of this document between CODE START and CODE END

### CODE START ###

# Verify centralized path configuration
/run scripts/path_config.py

# Verify HTMX full page reloads in admin interface
/run scripts/verification/verify_htmx_integration.py --full --test-stipends-crud --admin-interface

# Verify required fields for stipend creation
/run scripts/verification/verify_db_schema.py --strict --tables=stipend --validate-required-fields

# Security audit focused on admin interface
/run scripts/verification/verify_security.py --full-audit --check-admin-interface

# Verify test coverage for critical paths
/run scripts/verification/verify_test_coverage.py --module=stipend --threshold=85 --critical-paths

### CODE END ###

# Console commands done, we go on to 03_STEP_2.txt
/load automation/03_STEP_2.txt
