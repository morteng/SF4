[pytest]
testpaths = tests/
addopts = -v -x
python_files = test_*.py
filterwarnings =
    ignore::DeprecationWarning
    ignore::ResourceWarning
    ignore::UserWarning
markers =
    csrf: mark test as requiring CSRF protection
