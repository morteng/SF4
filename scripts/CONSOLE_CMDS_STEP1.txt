# --------------------------------------
# CONSOLE_CMDS_STEP1.txt
# --------------------------------------
# Console commands for Step 1.
# ONLY EDIT BETWEEN "COMMANDS START" and "COMMANDS END".

### COMMANDS START ###
/run pytest tests/auth/test_login.py -v --log-cli-level=DEBUG --cov=app --cov-report=term-missing
/run git branch -m main
/run git tag v0.1.0
/run git push origin main --tags
### COMMANDS END ###

# Once we're done, jump to Step 2.
/load scripts/MAIN_CMDS_STEP2.txt
