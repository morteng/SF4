# Console Commands for Step 3
# Do not use echo commands
# Only edit the section of this document between CODE START and CODE END

### CODE START ###
# Run version validation tests with coverage
/run pytest tests/version_management/test_version.py --cov=scripts.version --cov-report=term-missing

# Test database backup with logging
/run python -c "from scripts.version import create_db_backup; import logging; logging.basicConfig(level=logging.INFO); create_db_backup('instance/stipend.db')"

# Verify version file integrity
/run python -c "from scripts.version import validate_version_file; print(f'Version file valid: {validate_version_file()}')"

# Check test coverage
/run pytest --cov=scripts.version --cov-report=term-missing

# Check Git status
/run git status
### CODE END ###

# Print the current datetime to console
/run scripts/print_datetime.py
/run Write-Host "Code Cycle Step 3 Ends"

# Update TODO based on results
/code update TODO if needed. Do not explain or summarize, just output the edit.

# After running any console commands, go to STEP_4
/load scripts/STEP_4
