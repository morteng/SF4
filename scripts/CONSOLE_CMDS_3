# Console Commands for Step 3
# Do not use echo commands
# Only edit the section of this document between CODE START and CODE END

### CODE START ###
# Fix pytest.ini merge conflict
/run git checkout -- pytest.ini

# Run version management tests with coverage
/run pytest tests/version_management/ --cov=scripts.version --cov-report=term-missing -v

# Validate database connection
/run python -c "from scripts.version import validate_db_connection; import logging; logging.basicConfig(level=logging.DEBUG, format='%(asctime)s - %(levelname)s - %(message)s'); assert validate_db_connection('instance/stipend.db'); print('Database connection validation passed')"

# Create database backup
/run python -c "from scripts.version import create_db_backup; create_db_backup('instance/stipend.db')"

# Validate version file
/run python -c "from scripts.version import validate_version_file; assert validate_version_file()"

# Check Git status
/run git status
### CODE END ###

# Print the current datetime to console
/run scripts/print_datetime.py
/run Write-Host "Code Cycle Step 3 Ends"

# Update TODO based on results
/code update TODO if needed. Do not explain or summarize, just output the edit.

# After running any console commands, go to STEP_4
/load scripts/STEP_4
