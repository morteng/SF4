# Console Commands for Step 3 - Testing
# Only edit the section between CODE START and CODE END

### CODE START ###
# Initialize core systems with enhanced logging
/run scripts/path_config.py --verbose
/run scripts/init_logging.py --debug

# Setup test environment with proper app context
/run scripts/setup_test_env.py --test --app-context
/run scripts/install_test_dependencies.py --test --retry=3

# Initialize test database with schema validation
/run scripts/startup/init_db.py --validate-schema --force
/run scripts/startup/init_admin.py --verify-email --check-password --app-context

# Run comprehensive test suites with coverage
/run scripts/run_tests.py --unit --cov
/run scripts/run_tests.py --integration --cov
/run scripts/run_tests.py --e2e --cov

# Generate and verify coverage with module breakdown
/run scripts/generate_coverage_report.py --html
/run scripts/verify_test_coverage.py --critical --min-coverage=80 --module-coverage

# Run final test verification with extended checks
/run scripts/verify_tests.py --final --validate-schema
/run scripts/verify_deployment.py --test --check-environment

# Verify test environment cleanup with extended checks
/run scripts/verify_test_cleanup.py --strict

# Verify critical modules coverage with extended thresholds
/run scripts/verify_test_coverage.py --critical-modules --min-coverage=85

# Verify git state after testing with auto-resolve
/run scripts/verify_git_state.py --retry=5 --auto-resolve

### CODE END ###

# Proceed to Step 4 - Review
/load scripts/STEP_4
