# Console Commands for Step 2 - Coding
# Do not use echo commands
# Only edit the section between CODE START and CODE END

### CODE START ###
# Initialize core systems with enhanced logging
/run scripts/path_config.py
/run scripts/init_logging.py

# Verify git state is clean
/run scripts/verification/verify_git_state.py --pull --retry=3 --auto-resolve

# Initialize database with schema validation
/run scripts/startup/init_db.py --validate-schema --check-migrations

# Verify security settings with enhanced checks
/run scripts/verification/verify_security.py --check-env --validate-keys --verify-permissions

# Verify database connection with retry logic
/run scripts/verification/verify_db_connection.py --retry=5 --timeout=15

# Verify backup system integrity
/run scripts/verification/verify_backup.py --verify-size --check-schema

# Verify Render deployment readiness with full checks
/run scripts/verification/verify_render_ready.py --check-api --validate-vars --verify-backup

# Initialize admin user with enhanced validation
/run scripts/startup/init_admin.py --verify-email --check-password

# Commit pending changes with deployment message
/run scripts/commit_changes.py --message "Enhanced pre-deployment verification completed"

### CODE END ###

# Proceed to Step 3 - Review
/load scripts/STEP_3
