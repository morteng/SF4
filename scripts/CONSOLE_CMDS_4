# Console Commands for Step 4 - Review
# Only edit the section between CODE START and CODE END

### CODE START ###
# Initialize core systems
/run scripts/path_config.py
/run scripts/init_logging.py

# Fix critical security issues
/run scripts/fix_security_permissions.py --strict --verify --reset
/run scripts/verify_security.py --check-env --validate-keys --verify-permissions --check-rotation

# Verify git state is clean
/run scripts/verify_git_state.py --pull --retry=3 --auto-resolve

# Validate database configuration and connection
/run scripts/verify_db_config.py --validate-uri --check-permissions --strict
/run scripts/verify_db_connection.py --retry=3 --timeout=10 --validate-schema --critical

# Initialize database with enhanced validation
/run scripts/startup/init_db.py --validate-schema --test --migrate --backup-first --reset

# Verify Render deployment readiness
/run scripts/verify_render_ready.py --check-api --validate-vars --test-deploy --critical

# Finalize documentation with coverage reports
/run scripts/finalize_docs.py --strict --validate --with-coverage --critical

# Generate deployment checklist
/run scripts/generate_deployment_checklist.py --validate --strict --critical

# Request final approval
/run scripts/request_approval.py --final --validate --critical

# Final verification
/run scripts/verify_deployment.py --test --check-security --verify-backups
/run scripts/verify_tests.py --final --validate-all --critical

### CODE END ###

# Proceed to Step 5 - Deployment
/load scripts/STEP_5
