# Console Commands for Step 4 - Review
# Only edit the section between CODE START and CODE END

### CODE START ###
# Initialize core systems with enhanced logging
/run scripts/path_config.py --validate
/run scripts/init_logging.py --debug

# Verify git state with conflict resolution
/run scripts/verify_git_state.py --auto-resolve --retry=3 --pull

# Verify database configuration
/run scripts/verify_db_config.py --validate-uri --check-migrations
/run scripts/verify_db_connection.py --retry=3 --timeout=10

# Initialize test environment with proper context
/run scripts/setup_test_env.py --clean --with-context

# Initialize database with schema validation
/run scripts/startup/init_db.py --validate-schema --test --migrate

# Initialize admin user with strict validation
/run scripts/startup/init_admin.py --verify-email --check-password --validate-complexity --test

# Run comprehensive test suite
/run scripts/run_tests.py --unit --integration --e2e --coverage --retry=3

# Fix test coverage reporting
/run scripts/fix_coverage_reporting.py

# Generate detailed coverage reports
/run scripts/generate_coverage_report.py --html --xml --show-missing

# Verify coverage with module thresholds
/run scripts/verify_test_coverage.py --min-coverage=80 --critical-modules=app/models:90,app/services:90

# Finalize documentation with validation
/run scripts/finalize_docs.py --strict --validate
/run scripts/generate_deployment_checklist.py --validate --strict

# Verify deployment readiness
/run scripts/verify_deployment.py --test --check-security
/run scripts/verify_render_ready.py --strict --validate-vars

# Request final approval
/run scripts/request_approval.py --final --validate

# Final verification with cleanup
/run scripts/verify_tests.py --final --validate-all
/run scripts/verify_review.py --strict --validate
/run scripts/verify_test_cleanup.py --strict --validate

### CODE END ###

# Proceed to Step 5 - Deployment
/load scripts/STEP_5
